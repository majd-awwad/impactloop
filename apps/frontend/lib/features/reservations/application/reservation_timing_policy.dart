const minPickupNotice = Duration(minutes: 60);
const minCustomPickupStartNotice = Duration(minutes: 30);

const pickupWindowTooCloseMessage =
    'This pickup window is too close to ending. Propose a new time.';
const learnerPickupWindowTooCloseMessage =
    'Preferred pickup window is too close to ending. Choose a window with at least 60 minutes remaining.';
const proposedPickupStartTooSoonMessage =
    'Proposed pickup time must start in the future.';
