void openAdminVerificationDocumentBytes({
  required List<int> bytes,
  required String mimeType,
  required String filename,
}) {
  // Native/desktop admin review opens documents on web; no-op elsewhere.
}
