/// Minimum public detail views before showing a Popular badge.
const materialPopularViewsThreshold = 10;

/// Number of category chips shown before the "More" control on discovery browse.
const discoveryVisibleCategoryCount = 8;
